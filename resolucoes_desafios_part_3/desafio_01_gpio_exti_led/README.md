# Desafio 1 - GPIO + EXTI com debounce

Resolucao objetiva para o desafio de botao com EXTI, debounce por software e toggle de LED.

Arquivos:

- `solucao.c`: exemplo completo com ISR curta, debounce por `HAL_GetTick()` e processamento no `while(1)`.
